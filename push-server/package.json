{
  "name": "deny-push-server",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that sends APNs alert pushes for deny friend-request events.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
